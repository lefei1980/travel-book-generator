# Nominatim Geocoding Configuration
# Nominatim requires a valid contact email in the User-Agent header
# Replace with your real email address for production use
CONTACT_EMAIL=your-email@example.com

# Set to 'true' to use mock geocoding (for testing without external API)
MOCK_GEOCODING=false

# CORS Configuration
# Comma-separated list of allowed frontend origins
# For local dev: http://localhost:3000
# For production with Oracle VM: https://your-app.vercel.app
# Note: Add your Vercel domain here after deploying frontend
ALLOWED_ORIGINS=http://localhost:3000,https://your-app.vercel.app
